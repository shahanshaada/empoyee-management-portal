export default class Employee {
  constructor({ id, name, email, role, department }) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.role = role;
    this.department = department;
  }
}
